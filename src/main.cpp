#include "ply_parser.h"
#include <fstream>
#include <sstream>
#include <stdexcept>
#include <cctype>
#include <algorithm>
#include <string>
#include <iostream>

int main() {

    /**try {
        arap::Mesh mesh = arap::PLYParser::load("data/sample_cube.ply");

        if (!mesh.is_valid()) {
            std::cerr << "Error: Loaded mesh is empty." << std::endl;
            return 1;
        }
    } catch (const std::exception& e) {
        std::cerr << "Error loading PLY file: " << e.what() << std::endl;
        return 1;
    }*/
    return 0;
}